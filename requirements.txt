# Pulse · dependencies consolidated for the whole team
# Install with: pip install -r requirements.txt
# (or run scripts/setup.ps1 / scripts/setup.sh which also handles .env and node deps)

# === apps/api · FastAPI + chat ===
fastapi>=0.110
uvicorn[standard]>=0.27
sqlmodel>=0.0.16
psycopg2-binary>=2.9
alembic>=1.13
pydantic-settings>=2.2
google-genai>=1.0
httpx>=0.27
python-dotenv>=1.0

# === apps/ml · ETL + signals engine ===
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
duckdb>=0.10
sqlalchemy>=2.0
openpyxl>=3.1

# === extras opcionales (E0 estacionalidad) ===
statsmodels>=0.14

# === dev tools ===
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.3
mypy>=1.9
jupyter>=1.0
ipykernel>=6.29
